# Complex Current State

current_version: 2026-07-21

## Recovery Anchor

Complex uses one seven-behavior spine supported by four coupled engineering layers: Prompt Contract, Context Working Set, Runtime Harness, and Progress Loop. The layers diagnose failure; they are not four linear gates.

The active architecture has three universal mechanisms, four supporting practices, and four conditional extensions. Normative role and evidence status remain separate: the core and supporting practices are `tested`; the four conditional extensions remain `screened`; none is `validated`.

## Active Decisions

- Restore one authoritative project state before routing.
- Keep durable Goal and completion criteria stable; keep changing facts in the current working set.
- Treat project nature as a durable prior and let the active phase, module, or work item control the current beat when its work shape differs.
- Define the artifact contract before assigning tools, agents, lanes, threads, or workflows.
- Compile the smallest sufficient topology from manager work, deterministic Harness, temporary workers, recurring lanes, clean evaluation, and exact responsibility handoff.
- Keep one controller responsible for accepted state, integration, routing, and final acceptance when several resources are active.
- Move stable repeated judgment toward templates, checkers, tool contracts, or state machines; route assumption-driven mechanical failure back to discovery or strategic judgment.
- Treat the target project's completion predicate, artifact path, schema, fields, values, quantities, and verifier as the executable interface.
- Diagnose Prompt, Context, Harness, Loop, or model failure before adding prompt text.
- Keep project-internal decisions autonomous and place user intervention only at a responsibility-bearing operation or undelegated value judgment.
- Continue non-terminal `next_route` work until the outcome predicate or a true responsibility/tool boundary.
- Keep operating organization, portfolio control, trace appraisal, and external transfer conditional.
- Use evidence verbs precisely: examples illustrate, markers screen, fixtures reproduce, locked runs compare, and repeated real outcomes validate.

## Active Behavior Spine

1. Restore true state and assemble the Context Working Set.
2. Classify project nature as a prior, then route the active work unit.
3. Assign responsibility and decision rights.
4. Define the artifact contract and compile the minimum control, Harness, and topology.
5. Run a target-function Progress Loop that changes an outcome, exposes a decision, or falsifies a route.
6. Deliver for the intended reader and evaluate the outcome.
7. Leave accepted artifacts, layer diagnosis, recovery pointers, and `next_route`.

## Current Accepted Artifact

- scope: clarification of `project_nature_router`, `operating_organization`, and `portfolio_control_plane`; no new mechanism identity or gate.
- architecture: project-level nature is a prior; phase, module, and work-item routing can differ without changing the Goal.
- artifact-first contract: accepted input, operation or judgment, expected artifact or diagnostic outcome, acceptance, recovery, and state write-back precede resource allocation.
- topology: manager beat, deterministic Harness, temporary parallel workers, standing lane, clean evaluator, or exact responsibility handoff selected by current work shape.
- transitions: stable repeated judgment may mechanize; repeated deterministic failure caused by changed assumptions routes upward to discovery.
- external calibration: Magentic-One, Anthropic multi-agent research, Virtual Lab, and Agent Laboratory were used as architecture contrasts. They are not new pinned transfer records; the existing pinned Symphony, LangGraph, Codex, and SWE-agent records remain the machine-readable implementation basis.
- public/runtime integration: core, Skill, AGENTS, README, Quickstart, templates, explainer site, and the flagship portfolio example use the same abstraction.
- behavior coverage: 47 required cases and 47 transcript rules, including hierarchical work routing, artifact-first topology, and judgment/Harness transitions.
- evidence boundary: the revised example and behavior cases screen contract coverage. They do not show real-project superiority, so operating organization and portfolio control remain conditional and `screened`.
- retained empirical baseline: the 2026-07-17 seven-mechanism suite remains the latest locked comparison; its 84/84 valid trials reached the baseline ceiling and support no promotion.

## Active Goal

- thread_goal: Complete the Complex self-optimization that abstracts concrete domain workflows into artifact-first hierarchical routing and adaptive work topology, then align protocol, Skill, templates, examples, behavior screens, maturity, and recovery without adding a domain-specific organization or unsupported evidence claim.
- goal_surface_status: completed_in_current_task
- goal_resource_evidence: observed_in_current_task; resource=codex_goal(threadId=019f11f6-baaf-73a0-a72d-9a59c0ce2030); status=complete
- goal_completion_predicate: all public and runtime surfaces use the same abstraction; behavior and maturity links are bidirectional; no new mechanism identity is introduced; the flagship example is domain-neutral; full integrity and site checks pass; residual empirical limits remain explicit.
- active_module: none; adaptive topology clarification is complete.
- current_basis: 47/47 behavior and transcript screens pass; 11-mechanism maturity and architecture checks pass; eval/reference/recovery/integrity checks pass; explainer site builds; no evidence status changed.
- open_risks: no locked real-project comparison yet measures topology quality, coordination cost, correction load, or artifact lead time; the four architecture-contrast sources are not pinned transfer records.
- next_route: STOP_COMPLETE

## Completion Audit

- mechanism inventory remains 11: three core, four supporting practices, and four conditional extensions;
- no domain-specific role chart entered the core or Skill;
- the prior project-wide router and fixed lane list were replaced by hierarchical work routing and artifact-first topology selection;
- templates and the portfolio example show both downward mechanization and upward route-back;
- behavior pack and transcript rules are aligned at 47/47;
- mechanism, architecture, eval-record, reference-evidence, recovery, integrity, diff, and site-build checks pass;
- evidence status remains conservative: seven `tested`, four `screened`, zero `validated`;
- the next empirical unit is a locked real or redacted-real comparison of manager-only, fixed-role, and artifact-first compiled topology.

## Runtime Assets

- Core: `protocol/core.md`
- Skill: `.agents/skills/complex-runtime/SKILL.md`
- Templates: `templates/`
- Filled examples: `docs/examples/`
- Maturity: `docs/mechanism-maturity.json`
- Architecture decisions: `docs/active-architecture-rebaseline.json`
- External implementations: `docs/reference-implementation-evidence.json`
- Experiment program: `docs/evals/experiment-program.json`
- Behavior screens: 47 required cases and 47 transcript rules

## Stop Rule

Change a protocol default only when a repeated/high-impact project failure, discriminating experiment, verifier failure, user correction, or public onboarding failure supports the change. Otherwise keep the kernel stable and route the evidence gap to a narrower real-project test.
